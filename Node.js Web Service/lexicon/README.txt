Uses modern JavaScript (ES6 / ES2015) syntax and therefore requires Node v7+ and a modern browser (i.e. anything that isn't Internet Explorer!).

MongoDB has been dumped into the dump/ directory, and should be restored with database name lexicon (config.json is set to expect this db name, but you can change this along with the MongoDB port number).
